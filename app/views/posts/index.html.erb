<p id="notice"><%= notice %></p>

<table class="newsfeed">
  <thead>
    <tr>
      <th class="name">Name</th>
      <th>Posts</th>
      <th>Photos</th>
      <th colspan="3"></th>
    </tr>
  </thead>
<%= link_to 'New Post', new_post_path %>

  <tbody>
    <% @posts.each do |post| %>
      <tr>
        <td><img src="<%= post.user.profile_pic %>" alt="user profile picture" class="post-image"></td>
        <td><%= post.user_post %></td>
        <td><img src="<%= post.user_photo %>" alt="post" class="post-image"></td>
        <td><%# post.likes %></td>
        <td><%# post.dislike %></td>
        <td><%= link_to 'Show', post %></td>
        <td><%= link_to 'Edit', edit_post_path(post) %></td>
        <td><%= link_to 'Destroy', post, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

