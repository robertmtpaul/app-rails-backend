<%= form_with(model: posts_user, local: true) do |form| %>
  <% if posts_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(posts_user.errors.count, "error") %> prohibited this posts_user from being saved:</h2>

      <ul>
      <% posts_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :post_id %>
    <%= form.number_field :post_id %>
  </div>

  <div class="liked_field">
    <%= form.label :vote_post %><br>
    <%= form.number_field :vote_post %>
  </div>


  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
