<%= form_for([@post, @post.comments.build]) do |form| %>

  <div class="form-group">
    <%= form.text_area :content, :class=>'form-control', :placeholder=> 'Write a comment here' %>
  </div>

  <div class="actions">
    <%= form.submit :class=>'btn btn-secondary' %>
  </div>

<% end %>
